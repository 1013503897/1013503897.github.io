---
import Base from '../layouts/Base.astro';
import { getPosts, tagCounts } from '../lib/utils';
import { SOCIAL } from '../config';
const posts = await getPosts();
const tags = tagCounts(posts);
---
<Base title="关于" description="关于本站与作者">
  <section class="mx-auto max-w-3xl px-5 pt-14">
    <h1 class="text-2xl font-bold tracking-tight sm:text-3xl">关于</h1>
    <div class="prose prose-lg mt-6">
      <p>
        这里是我的移动安全 / 逆向工程技术手记。方向集中在
        <strong>加固脱壳、协议与签名算法逆向、RASP 与反作弊对抗、native 层纯算复现</strong>。
      </p>
      <p>
        每篇文章都尽量按真实分析顺序写，配上可复现的工具代码与证据图。
        脱敏口径：结构、公式、方法与参考实现照写，可照做复刻；仅对能直接当成品钥匙的
        完整密钥表、出口 IP、业务动机等做隐去或前缀脱敏。<strong>内容仅供技术交流。</strong>
      </p>
      <h2>研究栈</h2>
      <ul>
        <li><strong>静态</strong>：IDA Pro、jadx、LIEF + Capstone、自研反汇编统计与内存 trace</li>
        <li><strong>动态</strong>：Frida、unidbg（差分预言机）、KPM / 内核态无痕 hook、FART</li>
        <li><strong>复现</strong>：Python 离设备协议复现、逐字节对拍</li>
      </ul>
      <p>
        源码与更多项目在
        <a href={SOCIAL.github} target="_blank" rel="noopener">GitHub</a>。
        订阅更新可用 <a href="/rss.xml">RSS</a>。
      </p>
    </div>

    <div class="mt-8 grid grid-cols-3 gap-3">
      <div class="rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)] p-4 text-center">
        <div class="font-mono text-2xl font-bold text-[var(--color-accent)]">{posts.length}</div>
        <div class="mt-1 text-xs text-[var(--color-muted)]">篇文章</div>
      </div>
      <div class="rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)] p-4 text-center">
        <div class="font-mono text-2xl font-bold text-[var(--color-accent)]">{tags.length}</div>
        <div class="mt-1 text-xs text-[var(--color-muted)]">个标签</div>
      </div>
      <div class="rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)] p-4 text-center">
        <div class="font-mono text-2xl font-bold text-[var(--color-accent)]">看雪</div>
        <div class="mt-1 text-xs text-[var(--color-muted)]">主要投稿</div>
      </div>
    </div>
  </section>
</Base>
